# api_integration

An integration of API CRUD logics in a single app.

Here User has function of GET, PUT and DELETE while Login has POST logic. It fetches data from a fake API (https://jsonplaceholder.typicode.com) and works for CRUD logics here.

## Also Fixed Android network permission issues in Androidmanifest.xml for running in android.
